import QtQuick
import QtQuick.Controls
import EvolveUI

Page {
    background: Rectangle {
        color: "transparent"
    }

    Label {
        text: "Favorites Page"
        anchors.centerIn: parent
        font.pixelSize: 24
        color: theme.textColor
    }

}